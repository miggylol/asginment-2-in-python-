explanation 
first prompt the user to enter the diameter of a circle using the input function and convert the input to a float using float().
then tge program calculate the radius of the circle by dividing the diameter by 2, and then calculate the area and circumference of the circle using the radius and the math.pi constant.
then the program uses the print function to display the calculated area and circumference, rounded to two decimal places using the round function.
HUNNIDDOLLA
